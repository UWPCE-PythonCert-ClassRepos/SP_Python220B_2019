"""prints price info"""
def get_latest_price(item_code):
    """prints price info"""
    if item_code == 1:
        pass
        #to prevent pylint message about not using item_code
    return 24
    # Raise an exception to force the user to Mock its output
