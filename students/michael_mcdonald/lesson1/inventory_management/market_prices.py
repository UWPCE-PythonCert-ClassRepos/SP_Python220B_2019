"""Raise an exception to force the user to coverage its output"""

def get_latest_price(item_code=24):
    """get latest prices"""
    if item_code == 24:
        results_code = item_code
    else:
        results_code = 24
    return results_code
